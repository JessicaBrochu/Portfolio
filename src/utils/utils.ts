export const BASE_PATH = '/'
export enum Path {
    HERO = '#hero',
    PROJECTS = '#projects',
    ABOUT = '#about',
    CONTACT = '#contact'
}

// const {HERO, PROJECTS, ABOUT, CONTACT}

export const path = [

]